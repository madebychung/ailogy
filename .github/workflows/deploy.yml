name: test-trigger

on:
  push:
    branches: ["main"]
  workflow_dispatch:

jobs:
  echo:
    runs-on: ubuntu-latest
    steps:
      - name: test output
        run: echo "✅ GitHub Actions worked"
